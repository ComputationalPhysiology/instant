image: python:2.7

pipelines:
  default:
    - step:
        script:
          - apt-get update
          - apt-get install -y swig3.0 --no-install-recommends
          - pip install .
          - pip install pytest
          - cd test/
          - python run_tests.py
          - python rerun_tests.py