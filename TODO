TODO:
- Implement in-memory cache for already imported modules (based on 
signature)
  I.e. a global dict on the form
  _module_cache = { "module signature": previously_compiled_module }
  which is looked up transparently.

